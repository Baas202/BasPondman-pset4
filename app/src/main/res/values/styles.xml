<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPaletGreen</item>
        <item name="colorPrimaryDark">@color/colorGraphiteBlack</item>
        <item name="colorAccent">@color/colorGraphiteBlack</item>
    </style>

</resources>
